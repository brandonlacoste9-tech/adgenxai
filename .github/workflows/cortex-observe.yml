name: Cortex Observer

on:
  schedule:
    - cron: '0 * * * *'   # every hour
  workflow_dispatch: {}     # allow manual trigger

permissions:
  contents: read
  issues: write

jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch telemetry
        id: telemetry
        run: |
          curl -s https://adgenxai.pro/.netlify/functions/webhook-telemetry > telemetry.json
          echo "data<<EOF" >> $GITHUB_OUTPUT
          cat telemetry.json >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Post status to tracking issue
        uses: actions/github-script@v7
        env:
          TELEMETRY_DATA: ${{ steps.telemetry.outputs.data }}
        with:
          script: |
            const data = JSON.parse(process.env.TELEMETRY_DATA || '{}');
            const now = new Date().toISOString();
            const body = `## 🧠 Cortex Status Report

**Timestamp:** ${now}

### Metrics (Last 24h)
- **Total events:** ${data.stats?.totalEvents ?? 0}
- **Processing mode:** ${data.stats?.processing?.mode ?? 'observation'}
- **Status:** ${data.stats?.processing?.enabled ? '✅ Active' : '⏸️ Observation only'}

---
*Automated observation report — no actions taken*`;

            const issues = await github.rest.issues.listForRepo({
              owner: context.repo.owner, repo: context.repo.repo,
              labels: 'cortex-status', state: 'open', per_page: 1
            });

            if (issues.data.length) {
              await github.rest.issues.createComment({
                owner: context.repo.owner, repo: context.repo.repo,
                issue_number: issues.data[0].number, body
              });
              console.log('✅ Posted to issue #' + issues.data[0].number);
            } else {
              console.log('⚠️  No cortex-status issue found');
            }
